# Python file to run code, which was present in the .txt files so that i can get a practical experience.
# This is a test file, the code could be changed.

length = float(input("Length: "))
width = float(input("Width: "))
height = float(input("Height: "))

volume = length * width * height

print(f"The volume is: {volume}cm^3")